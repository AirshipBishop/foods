<%= form_for ([@restaurant, @review]), html: {class: "form-horizontal"} do |form|  %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <div class="row">
      <div class="col-md-4 col-md-offset-5">
        <h1>New Review</h1>
      </div>
   </div>
  </div>

<div class="container">
  <div class="form-group">
    <%= form.label :rating, {:class => "control-label col-sm-2"} %>
    <div class="col-sm-10">
      <%= form.number_field :rating, id: :review_rating, :class => "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="field">
      <%= form.label :comment, :class => "control-label col-sm-2" %>
      <div class="col-sm-10">
        <%= form.text_area :comment, id: :review_comment, :class => "form-control" %>
      </div>
    </div>
  </div>

  <div class="form-group">
    <div class="row">
      <div class="col-md-2 col-md-offset-2">
        <div class="actions">
          <%= form.submit :class => "btn btn-default" %>
        </div>
      </div>
    </div>
  </div>
</div>

<% end %>